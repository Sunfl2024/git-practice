# Git版本控制实践学习笔记

## 实践信息
- 姓名：孙凤琳
- 实践日期：2026年5月3日
# Git与代码版本管理实践笔记
24级 | 2026年春季学期 | 读书实践周任务

---

## 一、学习资料来源及相关链接
1.  Git 官方文档：https://git-scm.com/doc
2.  GitHub 官方使用指南：https://docs.github.com/zh
3.  菜鸟教程 Git 入门：https://www.runoob.com/git/git-tutorial.html

---

## 二、实践流程（安装、配置、使用过程）
### 1. Git 安装与环境配置
- 下载 Git 安装包：https://git-scm.com/downloads，按默认选项完成安装
- 配置全局用户名和邮箱（与 GitHub 注册邮箱保持一致）：
  ```bash
  git config --global user.name "Sunfl2024"
  git config --global user.email "你的邮箱@qq.com"

## 实践内容
本次实践完成了以下操作：

1. **本地仓库搭建**
   - 使用 `git init` 初始化仓库
   - 使用 `git add` 添加文件到暂存区
   - 使用 `git commit` 提交到本地仓库

2. **分支管理**
   - 使用 `git checkout -b` 创建并切换分支
   - 使用 `git merge` 合并分支

3. **远程仓库同步**
   - 在 GitHub 创建远程仓库
   - 使用 `git remote add` 关联远程仓库
   - 使用 `git push` 推送代码

## 关键命令记录
| 操作 | 命令 |
|------|------|
| 初始化仓库 | `git init` |
| 添加文件 | `git add 文件名` |
| 提交 | `git commit -m "说明"` |
| 查看状态 | `git status` |
| 查看历史 | `git log --oneline` |
| 创建分支 | `git checkout -b 分支名` |
| 合并分支 | `git merge 分支名` |
| 关联远程 | `git remote add origin 仓库地址` |
| 推送代码 | `git push -u origin master` |

1. 安装与配置
- 下载Git for Windows并安装
- 配置用户名和邮箱：
```bash
git config --global user.name "孙凤琳"
git config --global user.email "你的QQ号@qq.com"
```

2. 创建本地仓库

```bash
mkdir git-practice
cd git-practice
git init
```

3. 首次提交

```bash
echo "# Git实践笔记" > README.md
git add README.md
git commit -m "first commit"
```

4. 分支操作

```bash
git checkout -b feature-notes
echo "## 分支管理" >> README.md
git add README.md
git commit -m "add branch notes"
git checkout master
git merge feature-notes
```

5. 关联远程并推送

```bash
git remote add origin https://github.com/你的用户名/git-practice.git
git push -u origin master
```
##遇到的问题及解决方法

问题1：git config 报错 invalid key

· 原因：用了等号 user.name="姓名"
· 解决：改为空格 user.name "姓名"

问题2：GitHub推送认证失败

· 原因：GitHub已禁用密码认证
· 解决：使用 Personal Access Token 代替密码

学习心得

通过本次实践，我掌握了：

1. Git的基本工作流程：add → commit → push
2. 分支的创建、切换与合并
3. 本地仓库与远程仓库的关联与同步

Git是一个强大的版本控制工具，后续还需要继续学习解决冲突、rebase等高级操作。

## 实践总结
通过本次实践，我掌握了 Git 的基本工作流程，能够独立完成本地版本管理和远程仓库同步。