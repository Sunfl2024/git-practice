# Git版本控制实践学习笔记

## 实践信息
- 姓名：孙凤琳
- 实践日期：2026年5月3日

## 实践内容
本次实践完成了以下操作：

1. **本地仓库搭建**
   - 使用 `git init` 初始化仓库
   - 使用 `git add` 添加文件到暂存区
   - 使用 `git commit` 提交到本地仓库

2. **分支管理**
   - 使用 `git checkout -b` 创建并切换分支
   - 使用 `git merge` 合并分支

3. **远程仓库同步**
   - 在 GitHub 创建远程仓库
   - 使用 `git remote add` 关联远程仓库
   - 使用 `git push` 推送代码

## 关键命令记录
| 操作 | 命令 |
|------|------|
| 初始化仓库 | `git init` |
| 添加文件 | `git add 文件名` |
| 提交 | `git commit -m "说明"` |
| 查看状态 | `git status` |
| 查看历史 | `git log --oneline` |
| 创建分支 | `git checkout -b 分支名` |
| 合并分支 | `git merge 分支名` |
| 关联远程 | `git remote add origin 仓库地址` |
| 推送代码 | `git push -u origin master` |

## 实践总结
通过本次实践，我掌握了 Git 的基本工作流程，能够独立完成本地版本管理和远程仓库同步。